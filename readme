create folder database if does not exist
if exists
delete all folders inside database/
remove database/labels.mgp
remove database/trained_model.yml

git clone https://github.com/opencv/opencv.git
rename opencv to opencv-2.4.9

python face_recog.py store {name}
python face_recog.py train
python face_recog.py recog
